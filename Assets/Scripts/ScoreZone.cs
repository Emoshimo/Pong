using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreZone : MonoBehaviour
{
    public int id;

    void Update()
    {
        ScoreZone scoreZone = GetComponent<ScoreZone>();
        if (scoreZone) {
        }
    }
}
